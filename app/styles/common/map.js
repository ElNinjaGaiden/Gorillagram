import { StyleSheet } from 'react-native';

export default mapStyles = StyleSheet.create({
    mapContainer: {
        flex: 1
    },
    map: {
        position: 'absolute',
        top: 0,
        bottom: 0,
        left: 0,
        right: 0
    }
});